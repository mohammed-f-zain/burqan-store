Static files here are copied to the site root by Vite (see https://vitejs.dev/guide/assets.html#the-public-directory).

- burqanlogo.png — default logo used by BrandLogo (replace with your file; keep the name or change LOGO_SRC in src/components/BrandLogo.tsx).
- logo.svg — optional simple vector placeholder you can delete if unused.

URLs in the browser look like: /assets/burqanlogo.png
