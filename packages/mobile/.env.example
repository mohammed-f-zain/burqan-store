# Default (dev + release): https://api.burqan.store — see resolveApiBase.ts

# Optional: staging or another remote API in dev
# EXPO_PUBLIC_API_FORCE_ENV=1
# EXPO_PUBLIC_API_URL=https://staging-api.example.com

# Only when YOU run the API on your Mac (npm run api:dev, port 4000):
# EXPO_PUBLIC_API_USE_LOCAL=1

# Legacy: fixed LAN IP without USE_LOCAL (still works if USE_LOCAL=1 or FORCE_ENV=1)
# EXPO_PUBLIC_API_URL=http://192.168.1.42:4000
